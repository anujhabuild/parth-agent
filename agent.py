#!/usr/bin/env python3
"""
agent.py — Terminal agent (thin entrypoint).

The implementation lives in the `parth` package. See
`parth/main.py` for the REPL entry point.
"""


if __name__ == "__main__":
    from parth.cli import main

    main()
