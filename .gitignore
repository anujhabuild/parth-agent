# Virtual environments
.venv/
venv/
env/
.env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Testing / type checking
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/

# Environment and secrets (never commit API keys)
.env
.env.*
!.env.example

# Claude / local tool settings
.claude/settings.local.json
.claude/

# Project skills (local config, not repo-level)
.skills/

# Editors / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Windows build outputs (regenerated by build.ps1 — never commit)
installer/Output/
# Legacy zero-install Python embed bundle (31 MB of binary blobs).
# Not part of the build path; kept out of the repo on purpose.
build_windows/
